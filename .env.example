# Cloudflare Web Analytics site token (from dash → Web Analytics → Manage site).
# Leave empty to disable the beacon (e.g. local `npm run dev`).
VITE_CF_BEACON_TOKEN=

# Optional: override GitHub Pages asset base (CI sets BASE_PATH=/<repo>/ by default).
# BASE_PATH=/
