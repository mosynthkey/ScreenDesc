# Google Analytics (gtag.js) measurement ID, e.g. G-XXXXXXXXXX.
# Leave empty to disable (e.g. local `npm run dev`).
VITE_GA_MEASUREMENT_ID=

# Optional: override GitHub Pages asset base (CI sets BASE_PATH=/<repo>/ by default).
# BASE_PATH=/
